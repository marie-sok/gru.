name: functional_unique_creator_kerry
description: gru app project
publish_to: "none"
version: 1.0.0+1


environment:
  sdk: 3.11.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  image_picker: ^1.0.7
  flutter_svg: ^2.0.7
  provider: ^6.0.7
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.4.12
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true