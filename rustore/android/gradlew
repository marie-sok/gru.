#!/usr/bin/env sh
set -eu

if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi

echo "Gradle is not installed. Install Android Studio/Gradle, or run:" >&2
echo "  flutter create --platforms=android --org com.marie.sok ." >&2
exit 1
