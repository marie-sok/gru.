
  server:
    port: 8081

  spring:
    data:
      mongodb:
        uri: mongodb://localhost:27017/gru_db

    redis:
      host: localhost
      port: 6379

  jwt:
    secret: "forest_river_sunset_ocean_birds_sky_palms_seaside_171210"
    expiration: 86400000

    spring:
      data:
        redis:
          host: localhost
          port: 6379
